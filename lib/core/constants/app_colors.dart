import 'package:flutter/material.dart';

/// Palette warna modern dengan nuansa Dark Sleek Glassmorphism
class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0F172A); // Deep Slate Navy
  static const Color surface = Color(0xFF1E293B);    // Slate Dark
  static const Color surfaceLight = Color(0xFF334155);
  static const Color cardBg = Color(0x991E293B);     // Semi-transparent

  // Accents
  static const Color primary = Color(0xFFE11D48);    // Record Red / Crimson
  static const Color primaryGlow = Color(0x66E11D48);
  static const Color accent = Color(0xFF6366F1);     // Indigo Accent
  
  // States
  static const Color pause = Color(0xFFF59E0B);      // Amber
  static const Color resume = Color(0xFF10B981);     // Emerald Green
  static const Color stop = Color(0xFFEF4444);       // Bright Red

  // Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Borders & Dividers
  static const Color border = Color(0xFF334155);
  static const Color borderHighlight = Color(0xFF475569);
}
